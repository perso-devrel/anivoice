# 현재 상태

- **모드:** 무한 자율 (Ralph harness 구동 중)
- **목표:** BACKLOG 의 P0 → P1 → 자가생성풀 순으로 끝없이 처리
- **회귀 기준:** `node .ralph/test/dub-flow.mjs` 가 `ALL VIDEOS DUBBED SUCCESSFULLY` 출력 (exit 0), exit 78 = quota exceeded (외부 한도, 코드 회귀 아님)
- **마지막 iteration:** #183 — TestPage integration state 중복 input 3개를 config 배열 + .map()으로 교체, INTEGRATION_INPUT_CLASS 상수 추출, dub-flow exit 77 upstream-down 지속
- **다음 루프가 기대하는 출발점:**
  - P0 전부 완료, P1 전부 완료, P2 32개 + 자가생성 53개 완료
  - 인라인 배열 hoisting 완료, 중복 JSX 제거 시리즈 완료, 컴포넌트 분해 완료, 중복 패턴 통합 진행 중
  - vitest 설정 완료, `npm run test`로 382개 유닛 테스트 실행 가능 (api/ 포함)
  - `npm run lint` → 0 errors, 0 warnings, 0 eslint-disable comments
  - `npm audit`: 9개 취약점 남음 (모두 @vercel/node 체인, breaking change 필요)
  - Perso API 500 상태 — exit 77 (upstream-down) / 78 (quota) 모두 외부 장애로 분류
  - 다음 P2 미완료: 다운로드 URL HEAD 검증, 다국어 회귀 (둘 다 API 필요)
  - 메인 번들 257KB, StudioPage 20.9KB, DashboardPage 17.2KB, PricingPage 10.2KB, SettingsPage 7.9KB, UsageChart 346KB (recharts lazy), StudioPage 456줄, DashboardPage 294줄, PricingPage 259줄, LibraryPage 219줄, SettingsPage 196줄
  - TODO/FIXME/XXX 주석 0개, `any` 타입 0개, src/ 내 console.* 호출 0개
- **알려진 잠재 이슈:**
  - Perso API 서버 에러 (500) + ENTERPRISE_QUOTA_EXCEEDED — 한도/서버 회복 시점 미정
  - 프로젝트 20개 이상인 경우 서버 사이드 필터링/페이지네이션 필요
  - dub-flow.mjs 는 Firebase 토큰 없이 호출하므로 createProject / deductCredits 단계는 직접 검증 못 함
  - 상세 페이지에 조회수/좋아요 미구현 (DB 컬럼 없음)
- **운영 메모:**
  - develop / main 영구 브랜치는 절대 삭제 금지 (`delete_branch_on_merge=false`)
  - develop → main PR 머지 시 `--delete-branch` 옵션 사용 금지 (사고 이력 있음)
  - 매 iteration 후 sleep 90 으로 Vercel 배포 대기
  - gh CLI 경로: `/c/Program Files/GitHub CLI`
